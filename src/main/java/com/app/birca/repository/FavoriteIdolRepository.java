package com.app.birca.repository;

import com.app.birca.domain.entity.FavoriteIdol;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FavoriteIdolRepository extends JpaRepository<FavoriteIdol, Long> {
}
